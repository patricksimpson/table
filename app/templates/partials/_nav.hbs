<nav>
  <ul class="nav sp">
    <li class="nav--item">
    {{#if auth.person}} 
    {{#if auth.person.isWaiting}}
      <button class="nav--link" {{action personCancelWaitingList}}>Cancel</button></li>
    {{else}}
      <button class="nav--link" {{action personJoinWaitingList}}>PING</button></li>
    {{/if}}
    {{/if}}
    <li class="nav--item">{{#link-to 'people' class="nav--link"}}Leaderboard{{/link-to}}</li>
    <li class="nav--item">{{#link-to 'completedGames' class="nav--link"}}Game History{{/link-to}}</li>
  </ul>
</nav>
